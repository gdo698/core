spring:
    datasource:
        driver-class-name: org.postgresql.Driver
        url: jdbc:postgresql://db.ohzztttkxcbmqqhupcsir.supabase.co:5432/postgres
        username: postgres
        password: vXOPzLyyGfjmSF0Y

    jpa:
        show-sql: true
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect

    flyway:
        enabled: true
        locations: classpath:sql
        baseline-on-migrate: true
        out-of-order: true
